# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Runtime catalog fingerprints cover the exported bytes, including line endings.
Website/content/versions/*/catalog.json -text
