{
  "seed": "FINISHER_COMBO_RANGED",
  "purpose": "finisher-audit",
  "act": 3,
  "duration": 11,
  "karate": 0,
  "audit": {
    "monster": "TurretOperator",
    "targetHp": 70,
    "playerHp": 1,
    "expectedHits": 5,
    "mode": "Fast"
  },
  "cues": [
    { "id": "audio_calibration", "steps": [{ "action": "audit_calibration" }] },
    { "id": "idle", "steps": [{ "action": "wait", "seconds": 1 }] },
    { "id": "reverse_ranged_combo", "steps": [{ "action": "move", "actor": "enemy", "target": "ninja", "move": "UNLOAD_MOVE" }] },
    { "id": "recovery", "steps": [{ "action": "wait", "seconds": 2 }] }
  ]
}
