# 单人不歪种验证 — 2026-09-26

## 范围和来源

- 基线：已发布 0.3.2，`7ef64c976636f6d4963c893f01be80328be97972`。
- 本地分支：`codex/cfc-singleplayer-seeds`，隔离工作区 `C:/Users/theon/.codex/worktrees/cfc-singleplayer-seeds/NinjaSlayer`。候选是基线加本轮未提交差异，DLL 版本仍为 0.3.2；不能称作该干净 SHA 的发布包。原工作区的其他修改未覆盖。
- 参考：Temple9 的猫粮杯竞速比赛专用 Mod 1.1.2，Workshop 条目 `3774854959`。实际比较 DLL 的 SHA-256 为 `032894C8D157E9B667CE80BEFDC50DD0DE2E37189B5BD80C5A4F42A0B1D5014F`。
- 仅单人忍者杀手局启用。一人 Host／Client 仍属于联机；原版角色、多玩家局不初始化数据、不更换随机流。同装原版猫粮杯时让其补丁接管。
- 保留猫粮杯的原始计数时机、Boss 遭遇独立洗牌、父事件重入计数、400 项遗物稀有度序列。没有计时器、海选、固定路线或额外随机管理器。旧运行存档不迁移。
- 不改卡牌、资源、牌池、网站或编辑板；不推送 GitHub／Workshop。规则见 [机制说明](../singleplayer-seeds.md)。

## 双宿主候选

| 项目 | stable | preview |
|---|---|---|
| 宿主 | 0.107.1 | 0.111.0 |
| 宿主 MVID | `97f10687-c306-4798-ab75-8b9f23f34dfb` | `73b63ee0-6c0a-47bb-b0d1-b21f6d94222e` |
| 产品 Release 构建 | 通过，0 警告／0 错误 | 通过，0 警告／0 错误 |
| SmokeDriver Release 构建 | 通过 | 通过 |
| 全量候选 DLL 产品契约 | 通过，`stable-product-02.log` | 通过，`preview-product-01.log` |
| 必需补丁安装／回滚契约 | 通过，139 个目标／106 个关键目标 | 通过，140 个目标／107 个关键目标 |
| 单人随机规则产品契约 | 通过，`stable-seeds-02.log` | 通过，`preview-seeds-08.log` |
| 原猫粮杯 DLL 随机补丁对照 | 未运行，原 DLL 引用 preview 的 64 位 RNG API | 通过，3 个种子的完整测试轨迹一致 |

候选 DLL SHA-256：

- stable：`FCAC09FDC8376047E8E80543757BBCF1B434D15A5A2FD3EEDF4BD2965DA67E4B`
- preview：`30C0EC151CAFB9FD94EC83B1884FA9785393EB6FE8B9B65F645BEB6E8F4CC81C`

产品与契约编译使用锁定的 RitsuLib 0.5.12 SDK；本机游戏实际加载 RitsuLib 0.6.2。日志位于工作区 `build/seed-check/`。360 项逻辑测试通过，0 失败／0 跳过；兼容矩阵、仓库一致性和 diff 空白检查通过。

## 对照与隔离覆盖

契约加载实际候选 DLL，经 Ritsu 注册补丁，调用原生房间、敌人生成、洗牌、卡牌奖励、稀有度和药水接口。测试只跳过房间 UI 展示；这部分是产品契约，不是游戏实机。

使用 `CFC00`、`NINJA`、`ZZZZZZZZ`，每种重复运行，并在 preview 中卸载候选随机补丁后安装原猫粮杯 DLL 的随机补丁再对比。原 DLL 的运行数据在测试中按其开局规则初始化；没有启动比赛计时或海选。依赖版本绑定只存在于测试，产品不依赖或携带猫粮杯 DLL。

覆盖：400 项遗物稀有度及调用方随机流不被消耗、先古／商店／普通／精英／Boss／事件入口、实际敌人和牌序、卡牌生成、奖励与重掷、五种卡牌稀有度类别和保底、药水掉落与批次、父事件重入、保存数据序列化。重复使用原生奖励选项对象的行为也与原猫粮杯一致。

原版角色、一人 Host、一人 Client、双玩家及已安装原猫粮杯补丁时，检查开局不生成遗物序列、后续不修改存档计数或奖励随机流、原生生成结果不被替换。这些使用原生网络服务接口的测试替身，没有声称已运行真实联机客户端。

## 本机游戏重载

使用实际 stable 0.107.1 游戏、正常图形循环、RitsuLib 0.6.2 和本候选通用加载包。Steam 明确关闭，存档与配置隔离，测试副本路径绑定出站防火墙规则，遥测未授权。兼容渲染器为 `gl_compatibility`，本机 RTX 4060 Ti；不使用 ninja5080，不捕获截图。

`BossReload` 驱动真实新局、保存、退出进程、主菜单继续和第二 Boss。比较遭遇、HP、手牌、抽牌堆及完整随机状态。另先消耗一项遗物稀有度并调用一次原生药水批量生成，验证两次进程重启和同进程返回主菜单后，仍保留 **399 项遗物序列、1 次药水生成计数**，序列逐项一致。随后完成首个 Boss 并进入第二个 Boss。

证据目录为 `build/seed-check/live-boss-06/`，含原生游戏日志、检查点和启动器验证结果；此前 `live-boss-03/`、`live-boss-04/` 分别覆盖初始化状态及已消耗状态，均通过。最终机器可读指纹记录见 [证据清单](singleplayer-seeds-20260926-evidence.json)。

初轮环境问题单独保留：`live-boss-01` 在隐藏隔离启动下 Direct3D swap chain 初始化失败；`live-boss-02` 使用裸产品 DLL，缺少通用加载器对 Box2D.NET 的依赖解析，未进入玩法。改用兼容渲染器及现有通用加载包后完成游戏验证，未为这些环境错误修改产品。`live-boss-05` 是新增测试断言位置错误导致的 SmokeDriver 编译失败，修正后在第六轮重跑。第一次全量产品测试缺少新工作树中忽略的 Spine 原生 DLL，补齐既有依赖后通过。

headless 全量产品测试退出仍有 3 个 CanvasItem RID／ObjectDB 释放诊断，兼容渲染器保留宿主粒子功能限制提示；不将日志描述为零诊断。

## 未执行项

未运行 preview 图形游戏、真实多人进程、全程 AutoSlay、第三方完整模组组合或原猫粮杯比赛 UI。本轮仅验证并交付本地差异，不发布、不更改实际游戏安装目录，不宣称跨宿主的牌序相同。
