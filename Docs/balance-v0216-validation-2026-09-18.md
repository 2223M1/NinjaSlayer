# 0.2.16 卡牌与卡图验证

基线为 `39cd028347d0bc52f2669cb668220b9aae6f567a`。规格来自用户保存的 v1.13 编辑板及本轮确认：修行抽牌后每个实例弃一张；撒菱立即获得荆棘，分别持续三次敌方行动；奈落形态仅改文案。纳入77张卡图。强手里剑复用原版小刀的攻击命令顺序和0.2秒动作门槛，保留模组快速出牌加速。

## 实现与行为

- 修行使用原生独立 Power 实例，基础2／升级3空手道，原生手选弃牌与奇巧，不新增全局调度。
- 撒菱仍由原生 ThornsPower 反伤；独立倒计时仅扣回该次3／4荆棘，不移除其他来源荆棘。使用原生荆棘图标。
- 强手里剑在 AttackCommand 的命中特效位置创建原生 NShivThrowVfx 并替换手里剑外观，取消命中特效二次延迟排队。人物复用现有投掷动作；RapidCardPresentationContext 保持原样。
- 93张基础／升级卡的独立规格更新，奖励仍为20／35／25。目录与中英文卡牌、Power说明同步。

## 已执行

- 354项逻辑测试通过；仓库一致性、兼容配置及差异空白检查通过。
- stable 0.107.1、preview 0.111.0 产品和 SmokeDriver 构建通过。产品仍有既有泽渡两处可空性警告；SmokeDriver无警告。
- 双宿主完整 Orb 产品契约通过：混合基础／升级修行的原生回合抽牌、两次弃牌和嵌套奇巧；空手牌；原生荆棘反伤、跨三次敌方行动、错峰叠加到期及永久荆棘保留；小刀动作门槛；现有黑炎、奈落、南希、伤害快照及消耗回归。
- stable 0.107.1／RitsuLib 0.6.2／Vulkan 实机运行 `BalanceV0216`，13个检查点通过：三回合正常抽牌后手选弃牌、撒菱到期、永久荆棘保留、基础及升级强手里剑打出并消耗、十张调整牌卡图、正常退出。截图与录像在 `build/balance-v0216/live/run-B-04/`。
- 77张PNG全部解码，查看拼图和游戏内卡牌截图。

本地开发验证 DLL 位于 `build/balance-v0216/{stable,preview}/Release/NinjaSlayer.dll`。开发阶段日志的CandidateSha为基线，不作为最终不可变发布证明；最终提交的构建、宿主MVID、DLL校验值和远端包验证另记在 `build/releases/workshop-v0.2.16-evidence.json`。

未执行preview可视实机、双客户端ENet或完整AutoSlay。本轮实机沿用隔离本地存档、关闭Steam接入和针对隔离游戏进程的网络阻断。实机退出仍有既有Godot清理警告，不声称日志零错误。

初次测试发现的修正均在测试接线：stable／preview原生Hook和Damage重载差异；原生手选弃牌使用手牌选择器而非独立选牌屏幕；第二次弃牌会正常消耗奇巧生成的一层手里剑；枚举Power时新增空手道需要先取快照。
